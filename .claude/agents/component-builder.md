# Component Builder

Specializes in creating Angular standalone components following project conventions.
Generates `.ts`, `.html`, and `.css` files without the `.component` suffix.
Applies signals for reactive state and wires inputs, outputs, and template bindings correctly.
