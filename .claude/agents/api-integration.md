# API Integration

Specializes in HTTP services and external API consumption in Angular.
Creates typed services using `HttpClient`, handles error flows with RxJS operators, and maps API responses to domain models.
Advises on endpoint configuration, interceptors, and environment-based URL management.
