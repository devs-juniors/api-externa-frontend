# New Service

Scaffolds a new Angular injectable service in `src/app/`.
Creates a `<name>.service.ts` file with `providedIn: 'root'` and typed method stubs.
Injects `HttpClient` when the service will consume an external API.
