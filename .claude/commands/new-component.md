# New Component

Scaffolds a new Angular standalone component following project conventions.
Creates `<name>.ts`, `<name>.html`, and `<name>.css` files in the appropriate `src/app/` subdirectory.
Wires the component with signals for state and registers it in the relevant route or parent component imports.
